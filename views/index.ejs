<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>

   
    <header>
        <h1>Quora Posts</h1>
        <a href="http://localhost:3000/posts/new" class="new-post-btn">+ Create New Post</a>
    </header>

    
    <div class="post-container">
        <% for (post of posts) { %>
            <div class="post">
                <div class="post-header">
                    <h3 class="user">@ <%= post.username %></h3>
                </div>
                <p class="content"><%= post.content %></p>

                <div class="post-actions">
                    <a href="http://localhost:3000/posts/<%= post.id %>" class="btn view-btn">🔍 View</a>
                    <a href="http://localhost:3000/posts/<%= post.id %>/edit" class="btn edit-btn">✏️ Edit</a>
                    
                    <form method="post" action="http://localhost:3000/posts/<%= post.id %>?_method=DELETE" class="delete-form">
                        <button class="delete-btn">🗑 Delete</button>
                    </form>
                </div>
            </div>
        <% } %>
    </div>

</body>
</html>
